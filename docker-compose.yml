version: '3.8'
services:
  frontend:
    image: ${DOCKER_USER}/innovatech-frontend:latest
    ports:
      - "80:5000"
    environment:
      - BACKEND_URL=http://${BACKEND_HOST_PRIVATE_IP}:3000
    restart: always